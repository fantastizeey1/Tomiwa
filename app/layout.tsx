import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Cyphorix",
  description:
    "Cyphorix is a threat intelligence website that offers an interactive experience to uncover the methods of notorious APT groups. Discover their attack patterns and implement defense strategies in real-time simulations.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
